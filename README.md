# Snapshot Relay

A one‑page web tool that captures a live screenshot of any public webpage and returns a short, shareable link that displays the captured image instead of redirecting to the original URL.

## Stack
- **Frontend:** HTML, CSS, JavaScript (vanilla)
- **Backend:** Node.js + Express
- **Screenshot:** Puppeteer
- **Storage:** Firebase Storage + Firestore
- **Deployment:** Netlify (frontend), Render (backend)

## Live Demo
*(Will be added after deployment)*
